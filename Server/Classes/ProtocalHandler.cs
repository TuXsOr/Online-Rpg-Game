
namespace Server.Classes.Network
{
    internal static class ProtocalHandler
    {
        public static void HandleProtocol(ConnectedClient inClient, NetworkTransfer inData)
        {
            switch (inData.protocol)
            {
                default:
                    break;
            }
        }
    }
}
